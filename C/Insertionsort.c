#include "stdio.h"
void insert(int* element, int initial_location, int final_location)
{

}

int main()
{
    int n;
    printf("Enter size of array:");
    scanf("%d", &n);

    int arr[n];
    for(int i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    for(int i = 0; i < n; i++)
    {
        int j = i, k = 0;
        while()   
    }

    for(int i = 0; i < n; i++)
        printf("%d", arr[i]);
}